---
layout: null
---

{
  "posts": [
    {% for post in site.posts %}
    {
      "title": "{{ post.title }}",
      "link": "{{ site.baseurl }}/{{site.destination}}{{ post.url }}index.html",
      "date": "{{ post.date }}"
    }
    {% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}
